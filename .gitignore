*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
# phase-24-25 6.2: database dumps and file archives. A backup is every row in the system in one
# file, and the one place it must never end up is a repository that gets cloned, mirrored and
# pushed. Ignored as a directory so a new archive is never offered by .
/storage/app/backups
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# local workflow scripts (not part of the application)
.phase-*.js
.build-*.js

# local editor / preview config (not part of the application)
.claude/
